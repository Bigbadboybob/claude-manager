# Continuous Codex migration: execution record

The operator authorized continuing the migration overnight without per-task approval. On September 7 at approximately 14:22 UTC, after checking the sessions, the operator explicitly requested a fresh health check and the remaining migration if healthy. This authorizes advancing the cutovers before the former 24-hour pilot timing gate. Scheduled-cycle observation remains a separate validation item; it is not claimed complete. Preserve the rule that admitted work finishes before retirement. Do not describe pending engine changes or unobserved production cycles as complete.

## Current fleet: engine cutover complete

At the September 7 morning cutover checkpoint, all ten production definitions target Codex. All eight active orchestrator rollouts verify **CLI 0.153.4 / cm_pool / gpt-5.6-sol**, correct worktree, and the expected planning-task binding. The two originally paused tasks remain paused with no replacement session. The global session sweep found **no Claude sessions**. Daemon holder epoch 12 and PID 2893373 are unchanged, holder/brain registries agree, pending exits are zero, and the pool has had no service restart. The 15:31 real account probe passed. The final sweep checked 22 active-parent/worker rollouts, each on the required CLI/provider/model; `cm-codex-lb.service` is active with zero restarts.

| Task | First Codex observation | Remaining validation |
|---|---|---|
| api-update | Clean seq 75 completed; six review branches across seqs 74–75 preserved | Scheduled cadence and second clean productive cycle; seq 74's incorrect Orphaned status is not a pass |
| bug-triage | Seqs 586–587 completed, including a scheduled cycle; retained children reconciled | Normal ongoing artifact/operator review |
| perf-triage | Seqs 279–280 completed, including a scheduled cycle; retained children reconciled | Normal ongoing artifact/operator review |
| scraper-triage | Seqs 277–278 completed, including a scheduled cycle; review/monitoring state preserved | Normal ongoing artifact/operator review |
| code-bughunt | Seq 29 completed **handover/binding verification only**; baseline and three owner dispatches unchanged | Productive hunt on original 72-hour schedule; subsequent observations |
| momentum-detective | Seq 573 empty; scheduled seq 574 completed three real items | Second nonempty production batch |
| scraper-creation | Seqs 384–385 completed nonempty batches; seq 385 scheduled, both outcomes reviewed | Normal ongoing artifact/operator review |
| structured-scraper-creation | Seq 7 completed; scheduled seq 8 admitted and its worker still progressing | Second completed nonempty batch; continue artifact review |
| behavior-triage | Configured for Codex; remains paused; historical index recovered and reconciled | Productive validation deferred until explicit resume |
| scraper-opt | Configured for Codex; remains paused | Deferred until explicit resume |

All nine morning cutovers preserved the old `runs.jsonl` bytes as an exact prefix; the archive-prefix checks cover more than 5.7 MB of history. Stable configuration fields, schedules, identity, worktrees, enabled flags, supervision, retention and limits match the original snapshot. Run count, last run and next-fire timestamp were unchanged at each engine commit; only normal subsequent fires then advanced runtime state. No old staged consumer UUID was replayed. Final backups, expected-state requests, retirement barriers, engine operation IDs and proofs are under `fleet-cutover-1424Z/` in the private migration directory, mirrored locally.

Structured creation now binds the previously reserved parent **bf0aac2b-39c3-4479-a718-55b8f9015cc6**. Its old session was retired first; that same draft parent was activated and attached through `continuous.update`, then the engine commit and fresh launch verified the UUID. Actual children SP-9, SP-10 and SP-11 have distinct task IDs/worktrees and verified Codex/sol/pool rollouts. Further children continue through the saved backlog with three active worker slots. This proves the old missing-parent launch failure is repaired. SP-10 and SP-11 completed with all 11 validation checks passing, no warnings, and artifacts independently checked against their final branch heads (`889bb0a960` and `f2a9f7c6cf`). A separate read-only database transaction confirmed both records and SP-9 remain disabled. SP-9 was returned by its orchestrator for a timestamp-only duplicate-emission risk; the same worker corrected it at `dabd33db38`, added a focused regression and regenerated validation. That review/fix cycle remains ordinary admitted work, not a reason to force completion. Existing owner hard calls and disabled-record/review gates remain in force.

The independently running `scraperOptimization.service` (PID 2363677 observed under its systemd cgroup) remains running. It is an application producer, not an active continuous-task worker; the paused scraper-opt configuration cutover did not signal or restart it.

PredictionTrading prompt sources now reside on private branch `cm/continuous-codex-prompts-20260907` at **90c7ea905** (following `4aad0ab6e`). All ten live default prompts were applied during their own paused cutover. The detective template's reviewed Codex form is also preserved in task memory; its orchestrator renders the current repository brief and appends the required CM completion instruction, so normal main fast-forwards retain current task content and the completion requirement. The prompt source branch was subsequently integrated and pushed to PredictionTrading main at `08067c77f`; all ten live prompt bodies match the merged files. CM migration and pool-monitor source was integrated and pushed at `c61fe41`, preserving newer messaging and native-resume code. The tested daemon/MCP deployment remains unchanged at holder epoch 12.

## Runtime and validation

### Source, memory and artifact closeout, September 7, after 19:30 UTC

CM main `c61fe41` and PredictionTrading main `08067c77f` contain the migration work and are pushed to origin. CM integration preserves main's messaging and native transcript-resume changes. The merged workspace passed `cargo check --workspace`, **1,289 daemon library tests (four existing ignores), 51 daemon integration tests, 853 TUI tests, and 404 Python/MCP tests plus 26 subtests**. PredictionTrading's prompt selector passed **36 targeted tests**, plus targeted mypy and ruff. The merged sources match all ten live prompts byte-for-byte; no live prompt was bulk-reapplied. This closeout deployed only the read-only health watcher; no daemon, holder, LB, or application service was restarted.

The permanent watcher's first actual cron execution observed scraper-creation seq 385 complete and delivered its runtime milestone. Subsequent five-minute checks have zero component failures and no active incidents. The 19:30 real Codex probe returned OK and independently observed the exact required `cm_pool / gpt-5.6-sol / 0.153.4` runtime. At the latest runtime check holder epoch 12/PID 2893373 is stable, holder/brain each have 28 sessions, pending exits/unanswered pings are zero, and no production task is account-blocked or under recovery hold. Structured creation's seq 8 is Running with its SP-27 worker still doing admitted work. Originally paused tasks remain paused.

Behavior Triage's missing historical index was reconstructed privately from its last complete recorded file Read and 41 subsequent validated in-memory file edits, through August 25. Historical shell commands were not executed. The reconstructed YAML contains all six original finding IDs and later scope/rollback rulings. Current planning reads confirm five owner-closed findings and one blocked/fix-ready finding (BEH-002); the working index reconciles those statuses, archives five retained branch NOTES, and does not advance monitoring counters. The original reconstruction and operation/hash proof remain separate from the reconciled copy. The previous cycle log is not claimed fully recovered. The continuous definition's bytes are unchanged, and its task remains paused with no session. Current artifacts/metrics must still be reconfirmed when the owner resumes productive work.

Artifact review also exposed an inherited Perf Triage memory syntax error: cycle-199 appendix mappings followed a YAML sequence without a mapping header. A minimal header insertion makes the whole file parse while preserving every prior content line and all 38 issue entries. The idle task's original file and before/after hashes are backed up. This is a memory-format repair, not a scope or task-state change.

Scraper-creation seq 385's two original staged UUIDs map to recorded terminal outcomes: `diariocambio22.mx` received bounded investigation and a structural Cloudflare rejection (SC-146, clean retained branch with zero commits); `derwesten.de` was rejected under the existing recurring-value gate given the dedicated resolver and stronger pending source. No code was merged or deployed from those outcomes. Bug/Perf/Scraper triages' subsequent cycles legitimately found no new qualifying issue and preserved their review queues. API scheduled/full-cadence observation, a second nonempty Momentum batch, completion/review of the second Structured batch, and productive Code Bughunt observations on its original 72-hour schedule remain future gates. Runtime milestone notifications do not waive artifact or operator review.

Private closeout evidence lives under `health-watch-install/` in the migration archive, including cron backups, merged prompt hashes, worker reconciliation, historical/reconciled memory proofs and quota/runtime snapshots. No credentials, transcripts, private memory or queue payloads were committed.

### Closeout and permanent monitoring, September 7, 19:16 UTC

The existing session-independent `cm-notify` Telegram command passed an owner-requested real delivery test (HTTP 200). The new [health watcher](cm-health-watch.md) is installed on cm-manager and replaces only the temporary observer's marked five-minute cron entry. It has no expiry. The independent update checker and real account-probe schedules are unchanged. Pool-wide low/exhausted/unavailable alerts and recovery use durable deduplication; a single exhausted account produces no pool warning while another account has usable quota. Initial live check: all components passed, no active alerts, one exhausted account and 81% limiting headroom on the other. The pools share upstream quotas, so one remote notifier covers both hosts. Source tests cover 19 quota, monitoring, delivery and migration-evidence cases.

Two API workers had successful historical completion calls but missing current completion markers. Bounded status-only instructions to their existing sessions asked them to verify that no jobs/monitors remained and report completion again. Both did so through CM at 19:11 UTC (`ts-18d2f30ee1b7ff1f-8`, `ts-18d2f5502caffc44-6`); no raw session-state flags were edited. The original branches and completed work are retained.

The next scraper-creation batch (seq 385) started naturally at approximately 19:07 UTC. It remains admitted work and is not forced to finish. Holder epoch 12 and brain PID 2893373 remain unchanged; holder/brain registries agree, pending exits and unanswered pings are zero. Originally paused tasks remain paused. The permanent watcher records repeated/scheduled/nonempty evidence with the original production gates, excludes handover-only cycles, and sends runtime-readiness milestones separately from artifact/scope signoff. API's earliest full-cadence gate remains September 8 at 06:45:37 UTC; code-bughunt retains its original 72-hour schedule.


### Afternoon health check, September 7, approximately 18:50 UTC

All eight active definitions remain on Codex, with no in-flight run, account block, or recovery hold. Their latest runs are Done. All eight actual orchestrator rollouts still verify **0.153.4 / cm_pool / gpt-5.6-sol**. The two originally paused definitions remain paused. Holder epoch 12 and brain PID 2893373 are unchanged; holder and brain each report 37 sessions, with no pending exits or unanswered pings. MCP self-test passes. The pool service remains active with zero restarts, and the 18:30 real probe passed. One pool continuation-affinity error at 18:02 was followed by successful requests for that same conversation at 18:15; it is not a current fleet hold.

Observations since the morning table: Bug Triage seq 587, Perf Triage seq 280, and Scraper Triage seq 278 completed subsequent scheduler-token cycles. Momentum Detective seq 574 completed a real three-item batch, leaving its queue empty. Structured Scraper Creation seq 7 completed its first full Codex cycle. The other two consumers have no claimed items: scraper creation has two pending proposals whose one-hour window ends around 19:07 UTC; structured creation has six pending proposals whose six-hour window ends around 19:17 UTC. These are pending within their admission windows, not stranded batches.

Engine cutover is complete; closeout remains open for the slower scheduled observations, second nonempty consumer batches, artifact review, and source-change review/integration. Behavior Triage still needs its historical ignored index reconstructed before an explicitly requested productive resume. The five-minute migration observer is running without check errors, but expires on September 10; decide its permanent monitoring replacement before treating operational monitoring as closed out. Its diagnostic snapshot also retains two old API worker completion blockers even though both transcripts contain final answers and successful completion calls; reconcile that diagnostic evidence before using it as a retirement gate.

Both local and remote pools report the same shared quota state: one exhausted account and approximately 86% of the other account's reported weekly allowance remaining. No prospective whole-pool quota notification is installed. The installed codex-lb release has no outbound quota-notification setting; a separate read-only monitor can use reported quota windows/reset times and the existing owner notification command. Proposed behavior is one low-pool warning, a distinct whole-pool exhaustion alert, and a recovery notice with deduplication. Alert thresholds and installation remain a follow-up.

### Morning continuation, September 7

The 14:22 UTC health check found holder epoch 12 unchanged, matching holder/brain registries, no pending exits, and no new pilot failure or hold. The latest real probe verified cm_pool/gpt-5.6-sol/0.153.4. Both pool accounts were active; the main account had reset. The owner requested the remaining cutovers on this evidence, superseding the earlier wait for the pilot's 24-hour cadence. Preserve the scheduled observations as pending validation.

`bug-triage` drained and wrote a handover covering nine review lanes, their open owner decisions, and all six finished workers. Five workers belonged to the current parent; BUG-079 belonged to an older parent outside the drain descendant walk. All six freshly reported completion and confirmed no background/monitor obligations. Branch bundles, planning records, memory and final transcripts were saved; the older completed worker was retired separately and the guarded retirement closed the current generation. The engine commit preserved seq 585 and its schedule. Fresh Codex seq 586 launched at 14:36:43 UTC and its actual rollout verified the required provider, CLI and model.

Configuration-only cutovers for the two already-paused tasks were handled while that first live cycle was observed. This changed their place in the execution order to avoid needless idle time; it does not activate them or claim production validation. Behavior-triage's original managed branch was restored at the recorded commit. Its old ignored memory was absent before recovery; the new handover records that limitation and points to retained transcripts/current planning records rather than pretending to restore the original index.

Bug-triage seq 586 completed at 14:46:34 UTC, with memory cycle 496, no duplicate/new bug proposals, and two completed Codex child reconciliations in their retained task IDs/worktrees. The actual three rollouts verify cm_pool/0.153.4/gpt-5.6-sol. Independently verified clean review heads are BUG-104 `5e6945f8b` and BUG-110 `c8cd758ff`; no merge/push/deploy occurred. Both paused definition cutovers also completed and remain paused. Private evidence is under `fleet-cutover-1424Z/`.

After two production orchestrators completed real Codex worker cycles, the remaining legacy handovers were requested concurrently while keeping retirement and engine commits sequential. This is an execution-order adjustment under the morning direction to complete the remaining healthy cutovers: new normal Codex work may continue while another independent task migrates. All no-interruption, backup, reconciliation, runtime-proof, queue and original-pause guards remain required. Empty consumer queues leave nonempty-batch validation pending and do not require manufacturing work or blocking independent configuration cutovers.

cm-manager uses Codex CLI **0.153.4**, exact model **gpt-5.6-sol**, and the private **cm_pool** provider. Both accounts are enrolled. The account probe makes a real request through that provider and verifies the bound rollout. Local Codex receives no model pin.

The daemon library suite passed **1,246 tests, four ignored**, run serially because concurrent tests can contend on the global writer gate. The targeted prompt-source suite passed **23 tests**. `cargo check --workspace` and `git diff --check` passed. Releases were built outside the shared live target directory and installed with recorded hashes and backups. The deployed holder-run-adoption build has SHA-256 `6ff17b7d893053a30b4e8e3ebbcc363197cec195cbbc9a25600517bb5ceba46b`; holder epoch 11 remained healthy for over 15 minutes. The subsequent workflow delivery build has SHA-256 `b88ed5a70b560dc99d6cc690da46b27444fcff661dce12a0f9bc2b487881b9fa`; it is deployed at epoch 12, passed the live feedback retest, and remained healthy for over 15 minutes. Seq 75 remained Running through that deployment and subsequently completed.

Disposable tasks validated these behaviors:

| Check | Evidence |
|---|---|
| Persistent lifecycle | Four productive cycles reused the existing child task/worktree and preserved all artifacts. |
| Healthy drain | Drain rejected another fire while the admitted worker finished; final monitor, acknowledgment, checkpoint and completion settled naturally. |
| Investigator | The Fresh watchdog launched a Codex investigator, accepted `mark_unstuck` for healthy work, and the original run completed. |
| Missing completion | A real ended Codex turn without `report_done` became Failed. A consumer retained its staged batch under a recovery hold. |
| Partial recovery | A successful post-hold probe did not resume a drained task. Reconciliation classified both item IDs; after resume, only the unfinished original ID and dedupe key were replayed. All four ledger counts remained one. |
| Empty consumer | An empty manual cycle closed without another worker; the fixture is paused. |
| Compaction | Scheduled maintenance completed without claiming a batch, and the next productive cycle read memory correctly. CLI 0.153.4 kept the same rollout path and appended a compaction record; actual file rotation was not observed. |
| Restore | A completed fixture worker exited normally, then `session.revive` resumed the same post-compaction rollout. It recalled four artifacts before checking their files. |
| Active run across restart | A foreground 120-second canary stayed Running through the brain restart, logged `readopted`, then produced its exact artifact and `report_done` → Done. No orphan event occurred. |
| Engine migration | A paused fixture exercised Codex → Claude → Codex configuration commits, dry runs and duplicate retries without launching a Claude conversation. History and task identity were preserved. |

Two defects found by the canaries were repaired and retested: Codex must explicitly forward `CM_MONITOR_TRACKING_V1` into its MCP child; retained task bindings must reconstruct their original managed branch/workspace after idle workspace entries disappear during restart. Recovery rejects an unverifiable or unmanaged prior branch rather than creating an empty replacement.

The production pilot exposed a separate startup defect: the orphan sweep ran before holder adoption and recognized only legacy escrow or Claude-style process arguments. Codex processes survived the restart, but the sweep marked their active run Orphaned. Startup now includes holder survivors whose PID/start time and exit flags prove they are live. Regression tests reject reused PIDs, reaped children and pending exits. The live test above closes a gap in the original Phase C restart evidence, which had proved process survival without checking the active run status.

## Production preparation

All ten tasks have private snapshots outside their worktrees, including definitions, runtime evidence, planning children, memory and available transcripts. These are preparation snapshots, not quiescence proof; refresh each task after its drain. No credentials or `.env` files are included.

The `fleet-refresh-0618Z/` supplement includes structured creation's `.structured-scraper-creation` directory and scraper-opt's `.scraper-optimization` directory as well as daemon memory/run history. At preparation time the paused `behavior-triage` definition pointed to a missing worktree. The morning cutover restored the original `cm/behavior-triage` branch at `d3a428732729f437d8fcf3ed288ce4ce9db6ba4b`, retained its transcript/planning history, and kept it paused. Its absent historical ignored index still needs reconstruction before productive resume.

PredictionTrading branch `cm/continuous-codex-prompts-20260907`, commit `4aad0ab6e`, captures all ten prompt sources and maps them in `apply_orchestrator_prompt.py`. Existing task mandates and schedules remain intact. The detective worker brief now requires CM completion before its final response. The branch is staged, not merged or pushed; apply individual sources during their guarded cutover, never bulk-apply during the mixed-engine pilot.

`api-update` completed daemon seq 73 (productive memory cycle 62), then drained. Its old session wrote `.api-update/HANDOVER_CODEX.md`; the root handover points to that file and the existing operator rulings in `index.yaml`. Final memory, transcripts and child branch bundles are backed up. Legacy reconciliation, retirement and the engine commit passed. Fresh Codex pilot seq 74 began at 05:31:14 UTC; its actual rollout verifies cm_pool/gpt-5.6-sol/0.153.4.

Seq 74 received the incorrect Orphaned status described above. Its work continued uninterrupted: APIU-015 + APIU-013 item 1 finished at `98b2d821a` (needs evaluation/backtest); APIU-020 part B finished at `8d2450b91` (fix ready); APIU-012b rebased/re-gated at `a909c1cf4` (fix ready). All three final monitors delivered, the parent acknowledged/checkpointed drain, and each clean branch/commit was independently verified and bundled. These remain review branches; nothing was merged or deployed. The audit line remains Orphaned and does not count as a successful pilot cycle.

After reconciliation and the live startup-fix validation, normal-prompt seq 75 began at **06:13:57 UTC** with fire token `cm-api-update-codex-clean-pilot-20260907`, reusing the existing Codex parent. It completed at **06:45:37 UTC**, including final monitors and the three further approved workers: APIU-016 at `a51dc0967`, APIU-014 at `82a8862fe`, and APIU-010 at `31eb87e1a`. Each clean branch was independently verified and bundled; all remain blocked/fix_ready for operator review, with no merge or deploy. APIU-013 items 2–3–6 remain queued/unstarted. The parent reconciled memory cycle 64. The daily schedule and next natural fire at **2026-09-08 05:00:21 UTC** are unchanged.

At the 06:45 UTC pilot checkpoint the other nine engines were still Claude, and both deliberately paused tasks remained paused. Structured creation had the reserved draft parent `bf0aac2b-39c3-4479-a718-55b8f9015cc6`, subsequently bound by the morning cutover above. The actual recorder `--help` confirms `--actor`, `--record-slug` and `--validate-artifact-file`; validator `validate --help` imports successfully and exposes `--json` with the documented process-local `GCLOUD_PROJECT_ID=validate-only` workaround. No `.env`, record or database mutation was made by those prerequisite checks. Help/import success alone did not prove the specialized worker path. The morning cutover subsequently produced actual disabled-record artifacts as recorded above. Completed queue items must not be replayed.

## Workflow definitions

The three bundled workflow TOMLs were staged on cm-manager with Codex roles. An initial workflow fixture nevertheless launched Claude: the already-open local TUI republished its old in-memory definitions after the brain restart, overriding the installed base layer. The override layer was synchronized through `workflow.update_definitions`; a second fixture launched Codex reviewer and worker sessions. It completed the artifact, but both roles were recorded against one transcript, so this was not a validation pass.

Codex discovery now follows each session process's open rollout, and the workflow poller repairs an earlier incorrect binding rather than retaining it forever. The regression with two processes sharing one worktree passes. A live worker/reviewer/reviewer-return workflow completed with distinct owned transcripts and cm_pool/gpt-5.6-sol/0.153.4 for both roles (`workflow-owned-pass.json`).

The feedback fixture exercised worker V1 → fresh reviewer → manager → worker V2. Its second fresh-reviewer activation cleared context, but the next prompt was lost during Codex MCP startup; the workflow remained `RebindPending` and the second review never ran. The fixture was detached through `stop_workflow` without signaling its sessions. The default switch was held until repair and retest. The new deployed fix confirms against the process-owned post-clear rollout, explicitly excludes the old thread even under an isolated Codex home, and persists bounded delivery retries. A late successful turn suppresses retry input; exhausted retries pause the workflow with its pending prompt intact. Unit coverage passes. The live retest completed worker V1 → reviewer → manager → worker V2 → fresh reviewer → manager without operator rescue; the two reviewer activations used distinct transcripts, and all four role/history threads verified cm_pool/gpt-5.6-sol/0.153.4 (`feedback-fresh-fixed-pass.json`).

The same fixture also exposed a 40-second launch delay: two consecutive 20-second worktree spawn-slot waits exceeded the client timeout even though the workflow eventually started. The local fix removes workflow queue waits for Codex, whose process-owned discovery does not need snapshot ordering. Its occupied-queue regression and full suite pass; it is included in the epoch 12 delivery build. The repaired three-role launch returned in 0.027 seconds. Do not duplicate a timed-out launch without checking existing runs.

The installed `review`, `feedback` and `design-doc` definitions now select Codex for every role, and the effective override layer was synchronized. A subsequent review workflow launched without any engine override and completed its artifact with two distinct Codex/sol rollouts (`workflow-default-pass.json`). Production templates were retained: the staged worktree also differs in review worker `needs_mcp`, so copying its entire TOML would have changed more than engine defaults. That unrelated production template change was not applied.

The open TUI still retains its old definitions in memory. Before another brain restart, reload that TUI from the updated workflow sources or explicitly synchronize and reverify the override layer afterward. Do not interrupt active sessions to refresh it. A source file or installed TOML alone does not prove effective role selection.

## Evidence locations

Private artifacts are under `~/.cm/migrations/continuous-codex/20260907-pool-support/` on cm-manager, with selected copies on the operator's machine. `latest-checkpoint.json` now points to `fleet-cutover-1424Z/final-checkpoint.json`; the earlier `overnight-checkpoint.json` is retained as historical evidence. Key files include `active-codex-run-restart-pass.json`, `holder-run-adoption-deployment.json`, `consumer-real-recovery-pass.json`, `worker-post-compact-restore-pass.json`, `prompt-source-manifest.json`, `workflow-owned-pass.json`, `feedback-fresh-delivery-failure.json`, `feedback-fresh-fixed-pass.json`, `workflow-default-pass.json`, and `api-update-cutover/clean-pilot-completed/`. Completed continuous-canary sessions were archived and retired through guarded `continuous.retire`; all six exits were verified and the fixture definitions/history remain paused and retained. Keep transcripts and task memory out of source control.

The temporary read-only observer was replaced at 19:16 UTC by the permanent health watcher described above. Its private history and original crontab remain backed up. The new observer checks every five minutes without expiry, including pool quota and the exact repeated/scheduled/nonempty migration gates.

The API pilot's earliest full-cadence observation is still **2026-09-08 06:45:37 UTC (01:45 Chicago)**, after its next naturally scheduled cycle. This is now a production-validation checkpoint, not a gate blocking engine cutover: the morning operator direction authorized and completed all remaining engine changes. Scheduled and repeated-batch validation is explicitly open in the current-fleet table; the read-only observer does not certify artifact correctness automatically.
